---
tags:
  - project-file
  - claude-code
  - slash-command
aliases:
  - commands/example.md
---

# .claude/commands/example.md

## Overview

Placeholder slash command definition in `.claude/commands/`. Every `.md` file placed in this directory automatically becomes a `/filename` slash command that can be invoked during any Claude Code conversation in this project. The `example.md` is a template demonstrating the format — it should be deleted and replaced with real project commands.

**Path:** `.claude/commands/example.md`
**Owner:** Shared — committed to git
**Activates as:** `/example`

## Open Questions

- What project-specific slash commands are needed (e.g. `/review`, `/deploy`, `/document`)?

## Session Log

### 2026-04-27 — Placeholder created [wip]

- **What was done:** Created example.md as a template for future slash commands.
- **Decisions:** Kept generic so developers can see the format before creating real commands.
- **Notes / Caveats:** Delete this file when real commands are added; having `/example` as a live command in production is noisy.
- **Related:** [[skills-readme]], [[claude-agents-example]], [[skill-obsidian-vault-workflow]]
